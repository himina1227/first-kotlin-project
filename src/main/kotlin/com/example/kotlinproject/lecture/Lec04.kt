package com.example.kotlinproject.lecture

/**
 * 코틀린에서 연산자를 다루는 방법
 * 1. 단항 연산자 / 산술 연산자
 * 2. 비교 연산자와 동등성, 동일성
 * 3. 논리 연산자 / 코틀린에 있는 특이한 연산자
 * - in / !in
 *  - 컬렉션이나 범위에 포함되어 있다, 포함되어 있지 않다.
 * - a..b
 *  - a 부터 b 까지의 범위 객체를 생성한다.
 *  - <코틀린에서 반복문을 다루는 방법> 강의에서 다룬다.
 * - a[i]
 *  - a에서 특정 Index i로 값을 가져온다.
 *  결론적으로 자바와 같다 lazy 연산도 똑같다.(if 문에서)
 */
class Lec04 {
}