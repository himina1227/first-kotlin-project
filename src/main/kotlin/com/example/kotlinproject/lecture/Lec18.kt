package com.example.kotlinproject.lecture

/**
 * 코틀린에서 컬렉션을 함수형으로 다루는 방법
 * 1. 필터와 맵
 *  - filter, filterIndexed, map, mapIndexed, mapNotNull
 * 2. 다양한 컬렉션 처리 기능
 *  - any, none, all
 *  - count, sortedBy, sortedByDescending, distinctBy
 *  - first: 첫 번째 값을 가져온다(무조건 null이 아니어야함)
 *  - fistOrNull: 첫번째 값 또는 null을 가져온다(last, lastOrNull
 * 3. List를 Map으로
 *  - groupBy, assoiatedBy
 * 4. 중첩된 컬렉션 처리
 *  - flatten 중첩된 컬렉션을 풀어주는 역할을 한다.
 */
class Lec18 {
}